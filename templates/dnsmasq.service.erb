[Unit]
Description=DNS caching server.
After=network.target

[Service]
ExecStartPre=/usr/bin/bash -c 'echo "`hostname -i` `hostname`" > <%= @addn_hosts %>'
ExecStart=/usr/sbin/dnsmasq -k

[Install]
WantedBy=multi-user.target
